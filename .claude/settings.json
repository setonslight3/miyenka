{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -d node_modules ]; then npm install --no-audit --no-fund >/dev/null 2>&1; fi; if [ ! -f .env.local ]; then cp .env.example .env.local 2>/dev/null || true; fi",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
